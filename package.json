{
  "name": "pimpampum",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "concurrently -k \"pnpm --filter engine exec tsc --watch --preserveWatchOutput\" \"pnpm --filter web dev\"",
    "simulate": "pnpm --filter simulator start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.4.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
